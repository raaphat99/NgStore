import { NationalIDToBirthDatePipe } from './national-idto-birth-date.pipe';

describe('NationalIDToBirthDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalIDToBirthDatePipe();
    expect(pipe).toBeTruthy();
  });
});
